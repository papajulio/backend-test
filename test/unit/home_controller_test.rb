require 'test_helper'

class GroupTest < ActiveSupport::TestCase
  # I would have loved to create some tests to support my functions
  # but I didn't want to invest so many time learning the way to make this tests works with the controller
  # In any real project this is really worth it and you should invest time in it.
  test "the truth again" do
    assert true
  end
end
